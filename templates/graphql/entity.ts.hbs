import { {{ globalImports.typeOrm }} } from "typeorm";
import { {{ globalImports.graphQl }} } from "@nestjs/graphql";
import { {{ globalImports.entities }} } from ".";
import { {{ globalImports.decorators }} } from 'src/decorators';

@Entity()
@ObjectType({ description: "{{ entity.description }}" })
export class {{ entity.name }} {
  {{ objectFields }}
}

@InputType()
export class {{ entity.name }}CreateInput {
  {{ inputFields }}
}

@InputType()
export class {{ entity.name }}UpdateInput {
  {{ updateFields }}
}
